import React from "react";

interface MovieCineProps{
    title: string;
    imageSrc: string;
}

const Movie = (props: MovieCineProps): React.ReactElement =>{
    return(
        <>
        

        </>
    )
}